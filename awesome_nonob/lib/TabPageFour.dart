import 'package:flutter/material.dart';

class TabPageFour extends StatelessWidget {
  const TabPageFour({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("four"),
    );
  }
}
