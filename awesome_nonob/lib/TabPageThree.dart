import 'package:flutter/material.dart';

class TabPageThree extends StatelessWidget {
  const TabPageThree({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('three'),
    );
  }
}
