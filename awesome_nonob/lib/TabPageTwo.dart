import 'package:flutter/material.dart';

class TabPageTwo extends StatelessWidget {
  const TabPageTwo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TabPageTwo"),
    );
  }
}
